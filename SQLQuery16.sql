CREATE TABLE Courses (
    ID int NOT NULL,
    course_title varchar(255) NOT NULL,
    grade varchar(255), 
    PRIMARY KEY (ID)
);



INSERT INTO courses ( course_title, grade) VALUES ('online_strategies' ,'78');